# ATBS 前端技术栈说明

## 1. 前端技术栈概览

### 1.1 核心技术选型

| 技术类别 | 选型技术 | 版本 | 说明 |
|:--------|:--------|:----:|:-----|
| **框架** | Vue.js | 3.5.24 | 渐进式JavaScript框架 |
| **构建工具** | Vite | 7.2.5 | 下一代前端构建工具 |
| **语言** | TypeScript | 5.x | 类型安全的JavaScript超集 |
| **状态管理** | Pinia | 2.x | Vue官方推荐的状态管理方案 |
| **路由** | Vue Router | 4.x | Vue官方路由管理器 |
| **UI组件库** | Element Plus | 2.x | 基于Vue 3的组件库 |
| **HTTP客户端** | Axios | 1.x | 基于Promise的HTTP库 |
| **样式** | SCSS / CSS3 | - | CSS预处理器 |

### 1.2 开发工具链

| 工具类型 | 工具名称 | 用途 |
|:--------|:--------|:-----|
| **包管理器** | npm / pnpm | 依赖管理 |
| **代码规范** | ESLint + Prettier | 代码质量与格式化 |
| **版本控制** | Git | 源代码管理 |
| **API文档** | TypeScript类型定义 | 接口类型约束 |

## 2. 项目结构

```
atbs-app/
├── public/                     # 静态资源（不经过构建）
│   ├── favicon.ico
│   └── vite.svg
├── src/
│   ├── api/                    # API接口封装
│   │   ├── request.ts          # Axios请求封装
│   │   ├── auth.ts             # 认证相关接口
│   │   ├── user.ts             # 用户相关接口
│   │   ├── order.ts            # 订单相关接口
│   │   ├── airplane.ts         # 航班相关接口
│   │   └── prediction.ts       # 预测服务接口
│   ├── assets/                 # 静态资源（经过构建）
│   │   ├── images/
│   │   ├── icons/
│   │   └── styles/
│   │       ├── variables.scss  # SCSS变量
│   │       └── mixins.scss     # SCSS混入
│   ├── components/             # 公共组件
│   │   ├── common/             # 通用组件
│   │   │   ├── AppHeader.vue
│   │   │   ├── AppSidebar.vue
│   │   │   ├── AppFooter.vue
│   │   │   └── Loading.vue
│   │   ├── business/           # 业务组件
│   │   │   ├── FlightCard.vue
│   │   │   ├── OrderItem.vue
│   │   │   ├── PassengerForm.vue
│   │   │   └── PredictionChart.vue
│   │   └── ui/                 # UI组件封装
│   │       ├── AppButton.vue
│   │       ├── AppInput.vue
│   │       └── AppTable.vue
│   ├── composables/            # 组合式函数
│   │   ├── useAuth.ts          # 认证逻辑
│   │   ├── useUser.ts          # 用户逻辑
│   │   ├── useOrder.ts         # 订单逻辑
│   │   ├── useFlight.ts        # 航班逻辑
│   │   └── usePrediction.ts    # 预测逻辑
│   ├── directives/             # 自定义指令
│   │   ├── permission.ts       # 权限指令
│   │   └── loading.ts          # 加载指令
│   ├── layouts/                # 布局组件
│   │   ├── DefaultLayout.vue   # 默认布局
│   │   ├── AuthLayout.vue      # 认证布局
│   │   └── BlankLayout.vue     # 空白布局
│   ├── router/                 # 路由配置
│   │   ├── index.ts            # 路由入口
│   │   ├── routes.ts           # 路由定义
│   │   ├── guards.ts           # 路由守卫
│   │   └── modules/            # 路由模块
│   │       ├── auth.ts
│   │       ├── user.ts
│   │       ├── order.ts
│   │       ├── airplane.ts
│   │       └── prediction.ts
│   ├── stores/                 # Pinia状态管理
│   │   ├── index.ts            # Store入口
│   │   ├── modules/
│   │   │   ├── auth.ts         # 认证状态
│   │   │   ├── user.ts         # 用户状态
│   │   │   ├── order.ts        # 订单状态
│   │   │   ├── app.ts          # 应用状态
│   │   │   └── permission.ts   # 权限状态
│   │   └── plugins/
│   │       └── persist.ts      # 持久化插件
│   ├── types/                  # TypeScript类型定义
│   │   ├── api.ts              # API响应类型
│   │   ├── auth.ts             # 认证相关类型
│   │   ├── user.ts             # 用户相关类型
│   │   ├── order.ts            # 订单相关类型
│   │   ├── airplane.ts         # 航班相关类型
│   │   ├── prediction.ts       # 预测相关类型
│   │   └── global.ts           # 全局类型
│   ├── utils/                  # 工具函数
│   │   ├── index.ts            # 工具入口
│   │   ├── storage.ts          # 本地存储封装
│   │   ├── format.ts           # 格式化工具
│   │   ├── validate.ts         # 验证工具
│   │   ├── crypto.ts           # 加密工具
│   │   └── date.ts             # 日期工具
│   ├── views/                  # 页面视图
│   │   ├── auth/               # 认证页面
│   │   │   ├── Login.vue
│   │   │   ├── Register.vue
│   │   │   └── ForgotPassword.vue
│   │   ├── home/               # 首页
│   │   │   └── Index.vue
│   │   ├── flight/             # 航班模块
│   │   │   ├── Search.vue
│   │   │   ├── List.vue
│   │   │   └── Detail.vue
│   │   ├── order/              # 订单模块
│   │   │   ├── Create.vue
│   │   │   ├── List.vue
│   │   │   └── Detail.vue
│   │   ├── user/               # 用户中心
│   │   │   ├── Profile.vue
│   │   │   ├── Passengers.vue
│   │   │   └── Orders.vue
│   │   ├── prediction/         # 预测模块
│   │   │   ├── Dashboard.vue
│   │   │   └── Forecast.vue
│   │   └── error/              # 错误页面
│   │       ├── 404.vue
│   │       └── 500.vue
│   ├── App.vue                 # 根组件
│   ├── main.ts                 # 应用入口
│   └── env.d.ts                # 环境变量类型
├── .env                        # 环境变量
├── .env.development            # 开发环境变量
├── .env.production             # 生产环境变量
├── index.html                  # HTML模板
├── package.json                # 项目配置
├── tsconfig.json               # TypeScript配置
├── vite.config.ts              # Vite配置
└── README.md                   # 项目说明
```

## 3. 路由结构

### 3.1 路由配置表

| 路径 | 组件 | 名称 | 权限 | 说明 |
|:-----|:-----|:-----|:----:|:-----|
| / | Home | 首页 | - | 系统首页 |
| /login | Login | 登录 | - | 用户登录 |
| /register | Register | 注册 | - | 用户注册 |
| /flight/search | FlightSearch | 航班查询 | - | 搜索航班 |
| /flight/list | FlightList | 航班列表 | - | 航班结果列表 |
| /flight/:id | FlightDetail | 航班详情 | - | 航班详细信息 |
| /order/create | OrderCreate | 创建订单 | 登录 | 填写订单信息 |
| /order/list | OrderList | 订单列表 | 登录 | 我的订单 |
| /order/:id | OrderDetail | 订单详情 | 登录 | 订单详细信息 |
| /user/profile | UserProfile | 个人中心 | 登录 | 用户信息管理 |
| /user/passengers | Passengers | 乘机人管理 | 登录 | 管理乘机人 |
| /prediction | Prediction | 客流预测 | 登录 | 预测仪表盘 |
| /prediction/forecast | Forecast | 预测结果 | 登录 | 预测结果展示 |
| /404 | NotFound | 404 | - | 页面不存在 |

### 3.2 路由配置代码

```typescript
// src/router/routes.ts
import type { RouteRecordRaw } from 'vue-router'

export const routes: RouteRecordRaw[] = [
  {
    path: '/',
    name: 'Home',
    component: () => import('@/views/home/Index.vue'),
    meta: { title: '首页' }
  },
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/auth/Login.vue'),
    meta: { title: '登录', public: true }
  },
  {
    path: '/register',
    name: 'Register',
    component: () => import('@/views/auth/Register.vue'),
    meta: { title: '注册', public: true }
  },
  {
    path: '/flight',
    name: 'Flight',
    component: () => import('@/layouts/DefaultLayout.vue'),
    children: [
      {
        path: 'search',
        name: 'FlightSearch',
        component: () => import('@/views/flight/Search.vue'),
        meta: { title: '航班查询' }
      },
      {
        path: 'list',
        name: 'FlightList',
        component: () => import('@/views/flight/List.vue'),
        meta: { title: '航班列表' }
      },
      {
        path: ':id',
        name: 'FlightDetail',
        component: () => import('@/views/flight/Detail.vue'),
        meta: { title: '航班详情' }
      }
    ]
  },
  {
    path: '/order',
    name: 'Order',
    component: () => import('@/layouts/DefaultLayout.vue'),
    meta: { requiresAuth: true },
    children: [
      {
        path: 'create',
        name: 'OrderCreate',
        component: () => import('@/views/order/Create.vue'),
        meta: { title: '创建订单' }
      },
      {
        path: 'list',
        name: 'OrderList',
        component: () => import('@/views/order/List.vue'),
        meta: { title: '订单列表' }
      },
      {
        path: ':id',
        name: 'OrderDetail',
        component: () => import('@/views/order/Detail.vue'),
        meta: { title: '订单详情' }
      }
    ]
  },
  {
    path: '/user',
    name: 'User',
    component: () => import('@/layouts/DefaultLayout.vue'),
    meta: { requiresAuth: true },
    children: [
      {
        path: 'profile',
        name: 'UserProfile',
        component: () => import('@/views/user/Profile.vue'),
        meta: { title: '个人中心' }
      },
      {
        path: 'passengers',
        name: 'Passengers',
        component: () => import('@/views/user/Passengers.vue'),
        meta: { title: '乘机人管理' }
      }
    ]
  },
  {
    path: '/prediction',
    name: 'Prediction',
    component: () => import('@/layouts/DefaultLayout.vue'),
    meta: { requiresAuth: true },
    children: [
      {
        path: '',
        name: 'PredictionDashboard',
        component: () => import('@/views/prediction/Dashboard.vue'),
        meta: { title: '客流预测' }
      },
      {
        path: 'forecast',
        name: 'Forecast',
        component: () => import('@/views/prediction/Forecast.vue'),
        meta: { title: '预测结果' }
      }
    ]
  },
  {
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: () => import('@/views/error/404.vue'),
    meta: { title: '页面不存在' }
  }
]
```

## 4. 前后端交互流程

### 4.1 Axios封装

```typescript
// src/api/request.ts
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse } from 'axios'
import { useAuthStore } from '@/stores/modules/auth'
import { ElMessage } from 'element-plus'

// 创建axios实例
const request: AxiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器
request.interceptors.request.use(
  (config: AxiosRequestConfig) => {
    const authStore = useAuthStore()
    
    // 添加Token
    if (authStore.token) {
      config.headers = config.headers || {}
      config.headers.Authorization = `Bearer ${authStore.token}`
    }
    
    // 添加时间戳防止缓存
    if (config.method === 'get') {
      config.params = {
        ...config.params,
        _t: Date.now()
      }
    }
    
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response: AxiosResponse) => {
    const { code, message, data } = response.data
    
    // 业务成功
    if (code === 200) {
      return data
    }
    
    // 业务错误
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error) => {
    const { response } = error
    
    if (response) {
      switch (response.status) {
        case 401:
          // Token过期，跳转到登录
          const authStore = useAuthStore()
          authStore.logout()
          window.location.href = '/login'
          ElMessage.error('登录已过期，请重新登录')
          break
        case 403:
          ElMessage.error('没有权限执行此操作')
          break
        case 404:
          ElMessage.error('请求的资源不存在')
          break
        case 500:
          ElMessage.error('服务器内部错误')
          break
        default:
          ElMessage.error(response.data?.message || '网络错误')
      }
    } else {
      ElMessage.error('网络连接失败')
    }
    
    return Promise.reject(error)
  }
)

export default request
```

### 4.2 API接口定义

```typescript
// src/api/prediction.ts
import request from './request'
import type { 
  PredictionRequest, 
  PredictionResponse, 
  ModelInfo,
  TrainingTask 
} from '@/types/prediction'

/**
 * 预测服务API
 */
export const predictionApi = {
  /**
   * 获取客流预测
   * @param params 预测参数
   */
  getForecast(params: PredictionRequest): Promise<PredictionResponse> {
    return request.get('/api/mgmt/v1/prediction/forecast', { params })
  },

  /**
   * 创建训练任务
   */
  createTrainingTask(): Promise<TrainingTask> {
    return request.post('/api/mgmt/v1/prediction/train/async')
  },

  /**
   * 获取训练任务状态
   * @param taskId 任务ID
   */
  getTaskStatus(taskId: string): Promise<TrainingTask> {
    return request.get(`/api/mgmt/v1/prediction/train/task/${taskId}`)
  },

  /**
   * 获取模型信息
   */
  getModelInfo(): Promise<ModelInfo> {
    return request.get('/api/mgmt/v1/prediction/model-info')
  },

  /**
   * 获取自动重训状态
   */
  getAutoRetrainStatus(): Promise<any> {
    return request.get('/api/mgmt/v1/prediction/auto-retrain/status')
  }
}
```

### 4.3 交互流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                      前后端交互流程                               │
└─────────────────────────────────────────────────────────────────┘

    前端 (Vue3)                    后端 (Spring Cloud)
         │                                │
         │  1. 用户操作触发请求            │
         │  (点击查询/提交表单)            │
         │                                │
         ▼                                │
    ┌──────────┐                         │
    │  Vue组件  │                         │
    │ (Composition│                       │
    │   API)    │                         │
    └────┬─────┘                         │
         │                                │
         │  2. 调用Composables            │
         │  (业务逻辑封装)                 │
         │                                │
         ▼                                │
    ┌──────────┐                         │
    │ useFlight│                         │
    │ useOrder │                         │
    │usePrediction│                      │
    └────┬─────┘                         │
         │                                │
         │  3. 调用API接口                 │
         │  (Axios封装)                   │
         │                                │
         ▼                                │
    ┌──────────┐                         │
    │  API模块  │                         │
    │(TypeScript│                         │
    │ 类型约束) │                         │
    └────┬─────┘                         │
         │                                │
         │  4. HTTP请求                   │
         │  GET/POST/PUT/DELETE           │
         │──────────────────────────────►│
         │                                │
         │                          ┌──────────┐
         │                          │  Gateway │
         │                          │  (路由)  │
         │                          └────┬─────┘
         │                                │
         │                          ┌──────────┐
         │                          │ 微服务   │
         │                          │(业务处理)│
         │                          └────┬─────┘
         │                                │
         │  5. 返回响应                   │
         │  (JSON格式)                    │
         │◄──────────────────────────────│
         │                                │
         ▼                                │
    ┌──────────┐                         │
    │Axios拦截器│                         │
    │(统一处理) │                         │
    └────┬─────┘                         │
         │                                │
         │  6. 更新Pinia状态              │
         │  (响应式数据)                   │
         │                                │
         ▼                                │
    ┌──────────┐                         │
    │   Pinia  │                         │
    │  (Store) │                         │
    └────┬─────┘                         │
         │                                │
         │  7. 视图自动更新               │
         │  (Vue响应式)                   │
         │                                │
         ▼                                │
    ┌──────────┐                         │
    │  Vue视图  │                         │
    │ (模板渲染)│                         │
    └──────────┘                         │
```

## 5. 组件结构

### 5.1 组件依赖关系

```
src/components/
│
├── common/                     # 通用组件
│   ├── AppHeader.vue           # 依赖: Element Plus
│   ├── AppSidebar.vue          # 依赖: Element Plus, Vue Router
│   ├── AppFooter.vue           # 无依赖
│   └── Loading.vue             # 依赖: Element Plus
│
├── business/                   # 业务组件
│   ├── FlightCard.vue          # 依赖: Element Plus, 通用组件
│   ├── OrderItem.vue           # 依赖: Element Plus, 通用组件
│   ├── PassengerForm.vue       # 依赖: Element Plus, 验证工具
│   └── PredictionChart.vue     # 依赖: ECharts, Element Plus
│
└── ui/                         # UI组件封装
    ├── AppButton.vue           # 依赖: Element Plus Button
    ├── AppInput.vue            # 依赖: Element Plus Input
    └── AppTable.vue            # 依赖: Element Plus Table
```

### 5.2 组件通信方式

| 通信方式 | 使用场景 | 示例 |
|:--------|:---------|:-----|
| **Props/Emits** | 父子组件通信 | 父组件传递数据给子组件 |
| **Provide/Inject** | 跨层级通信 | 主题配置、用户信息 |
| **Pinia** | 全局状态共享 | 用户状态、认证状态 |
| **Event Bus** | 非父子组件通信 | 全局事件通知 |
| **Refs** | 直接访问组件实例 | 调用子组件方法 |

## 6. 状态管理

### 6.1 Pinia Store结构

```typescript
// src/stores/modules/auth.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { UserInfo, LoginForm } from '@/types/auth'
import { authApi } from '@/api/auth'

export const useAuthStore = defineStore('auth', () => {
  // State
  const token = ref<string>(localStorage.getItem('token') || '')
  const userInfo = ref<UserInfo | null>(null)
  
  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const username = computed(() => userInfo.value?.username || '')
  
  // Actions
  async function login(form: LoginForm) {
    const res = await authApi.login(form)
    token.value = res.token
    userInfo.value = res.userInfo
    localStorage.setItem('token', res.token)
    return res
  }
  
  async function logout() {
    await authApi.logout()
    token.value = ''
    userInfo.value = null
    localStorage.removeItem('token')
  }
  
  async function fetchUserInfo() {
    const res = await authApi.getUserInfo()
    userInfo.value = res
    return res
  }
  
  return {
    token,
    userInfo,
    isLoggedIn,
    username,
    login,
    logout,
    fetchUserInfo
  }
})
```

## 7. 构建配置

### 7.1 Vite配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  
  // 路径别名
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  
  // 开发服务器配置
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  },
  
  // 构建配置
  build: {
    target: 'esnext',
    outDir: 'dist',
    assetsDir: 'assets',
    
    // 代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          'element-plus': ['element-plus'],
          'echarts': ['echarts'],
          'vue-vendor': ['vue', 'vue-router', 'pinia']
        }
      }
    },
    
    // 压缩配置
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    }
  }
})
```

## 8. 技术栈选型理由

### 8.1 为什么选择Vue 3？

1. **Composition API**：更灵活的代码组织方式，更好的逻辑复用
2. **TypeScript支持**：更好的类型推断和IDE支持
3. **性能提升**：更小的包体积，更快的渲染性能
4. **生态成熟**：Element Plus、Vue Router、Pinia等生态完善

### 8.2 为什么选择Vite？

1. **开发体验**：极速的冷启动，即时的模块热更新
2. **构建速度**：基于esbuild，构建速度比Webpack快10-100倍
3. **原生ESM**：利用浏览器原生ES模块导入
4. **开箱即用**：内置TypeScript、JSX、CSS预处理器支持

### 8.3 为什么选择Pinia？

1. **官方推荐**：Vue官方团队开发，未来趋势
2. **TypeScript友好**：完整的类型支持
3. **简洁API**：更简单的API设计，学习成本低
4. **Devtools支持**：更好的调试体验

## 9. 总结

ATBS前端技术栈采用Vue 3 + TypeScript + Vite的现代前端技术组合，具有以下特点：

1. **高性能**：Vite构建 + Vue 3响应式系统
2. **类型安全**：TypeScript全栈类型约束
3. **模块化**：清晰的目录结构，职责分离
4. **可维护**：组合式API + Pinia状态管理
5. **工程化**：ESLint + Prettier代码规范
