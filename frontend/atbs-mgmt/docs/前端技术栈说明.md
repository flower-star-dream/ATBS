# ATBS 管理端前端技术栈说明

## 1. 项目概述

`atbs-mgmt` 是ATBS（航空票务预订系统）的管理后台前端项目，采用Vue 3 + TypeScript + Element Plus技术栈构建，为管理员提供完整的票务管理、航班管理、订单管理等功能。

## 2. 核心技术栈

### 2.1 核心技术选型

| 技术类别 | 选型技术 | 版本 | 说明 |
|:--------|:--------|:----:|:-----|
| **框架** | Vue.js | 3.4.0 | 渐进式JavaScript框架 |
| **构建工具** | Vite | 6.2.4 | 下一代前端构建工具 |
| **语言** | TypeScript | 5.3.0 | 类型安全的JavaScript超集 |
| **状态管理** | Pinia | 3.0.2 | Vue官方推荐的状态管理方案 |
| **路由** | Vue Router | 4.2.5 | Vue官方路由管理器 |
| **UI组件库** | Element Plus | 2.4.4 | 基于Vue 3的企业级组件库 |
| **图标库** | @element-plus/icons-vue | 2.3.1 | Element Plus官方图标 |
| **HTTP客户端** | Axios | 1.9.0 | 基于Promise的HTTP库 |
| **加密库** | crypto-js | 4.2.0 | JavaScript加密库 |
| **图表库** | ECharts | 6.0.0 | 数据可视化图表库 |
| **样式预处理器** | Sass | 1.87.0 | CSS预处理器 |

### 2.2 开发工具链

| 工具类型 | 工具名称 | 用途 |
|:--------|:--------|:-----|
| **包管理器** | pnpm | 依赖管理 |
| **代码规范** | ESLint + Prettier | 代码质量与格式化 |
| **类型检查** | vue-tsc | Vue单文件组件类型检查 |
| **自动导入** | unplugin-auto-import | API自动导入 |
| **组件自动导入** | unplugin-vue-components | 组件自动导入 |
| **版本控制** | Git | 源代码管理 |

## 3. 项目结构

```
atbs-mgmt/
├── public/                     # 静态资源（不经过构建）
│   └── vite.svg
├── src/
│   ├── api/                    # API接口封装
│   │   ├── index.ts            # API入口
│   │   ├── oauth2.ts           # OAuth2认证接口
│   │   ├── airplane.ts         # 飞机管理接口
│   │   ├── schedule.ts         # 航班管理接口
│   │   ├── order.ts            # 订单管理接口
│   │   ├── ticket.ts           # 票务管理接口
│   │   ├── employee.ts         # 员工管理接口
│   │   ├── user.ts             # 用户管理接口
│   │   ├── passenger.ts        # 乘机人管理接口
│   │   ├── seat-reservation.ts # 座位预订接口
│   │   ├── route.ts            # 航线管理接口
│   │   ├── station.ts          # 机场管理接口
│   │   ├── route-station.ts    # 航线站点接口
│   │   └── prediction.ts       # 客流预测接口
│   ├── assets/                 # 静态资源（经过构建）
│   │   └── atbs/
│   │       ├── logo.png        # 系统Logo
│   │       └── Avatar.png      # 默认头像
│   ├── components/             # 公共组件
│   │   ├── DialogForm/         # 对话框表单组件
│   │   │   ├── DialogForm.scss
│   │   │   └── DialogForm.vue
│   │   ├── Icon/               # 自定义图标组件
│   │   │   ├── EmployeeIcon.vue
│   │   │   ├── OrderIcon.vue
│   │   │   ├── PassengerIcon.vue
│   │   │   ├── ProfileIcon.vue
│   │   │   ├── ResetPwdIcon.vue
│   │   │   ├── RouteIcon.vue
│   │   │   ├── ScheduleIcon.vue
│   │   │   ├── SeatReservationIcon.vue
│   │   │   ├── TrainIcon.vue
│   │   │   └── TransportIcon.vue
│   │   └── ListPage/           # 列表页面组件
│   │       ├── ListPage.scss
│   │       └── ListPage.vue
│   ├── config/                 # 配置文件
│   │   ├── index.ts            # 配置入口
│   │   ├── constants.ts        # 常量定义
│   │   ├── env.ts              # 环境变量
│   │   ├── types.ts            # 配置类型
│   │   └── README.md           # 配置说明
│   ├── plugins/                # 插件
│   │   └── autoImportAllVue.ts # Vue全局自动导入
│   ├── router/                 # 路由配置
│   │   └── index.ts            # 路由定义
│   ├── stores/                 # Pinia状态管理
│   │   ├── index.ts            # Store入口
│   │   ├── app.ts              # 应用状态
│   │   ├── employee.ts         # 员工状态
│   │   └── oauth2.ts           # OAuth2认证状态
│   ├── styles/                 # 全局样式
│   │   └── global.scss         # 全局SCSS样式
│   ├── types/                  # TypeScript类型定义
│   │   ├── index.ts            # 类型入口
│   │   ├── client.ts           # 客户端类型
│   │   ├── oauth2.ts           # OAuth2类型
│   │   ├── employee.ts         # 员工类型
│   │   ├── user.ts             # 用户类型
│   │   ├── airplane.ts         # 飞机类型
│   │   ├── schedule.ts         # 航班类型
│   │   ├── order.ts            # 订单类型
│   │   ├── ticket.ts           # 机票类型
│   │   ├── passenger.ts        # 乘机人类型
│   │   ├── seat-reservation.ts # 座位预订类型
│   │   ├── route.ts            # 航线类型
│   │   ├── station.ts          # 机场类型
│   │   ├── route-station.ts    # 航线站点类型
│   │   └── prediction.ts       # 预测类型
│   ├── utils/                  # 工具函数
│   │   ├── base64.ts           # Base64编码解码
│   │   ├── formatDate.ts       # 日期格式化
│   │   ├── loadAsset.ts        # 资源加载
│   │   ├── md5.ts              # MD5加密
│   │   └── request.ts          # Axios请求封装
│   ├── views/                  # 页面视图
│   │   ├── 404.vue             # 404页面
│   │   ├── Login.vue           # 登录页面
│   │   ├── OAuth2Callback.vue  # OAuth2回调页面
│   │   ├── Layout.vue          # 布局组件
│   │   ├── Home/               # 首页
│   │   │   └── Home.vue
│   │   ├── Dashboard/          # 仪表盘
│   │   │   └── index.vue
│   │   ├── Airplane/           # 飞机管理
│   │   │   └── Airplane-list-view/
│   │   ├── Schedule/           # 航班管理
│   │   │   ├── Schedule-list-view/
│   │   │   └── Schedule-dialog-view/
│   │   ├── Order/              # 订单管理
│   │   │   └── Order-list-view/
│   │   ├── Ticket/             # 票务管理
│   │   │   └── Ticket-list-view/
│   │   ├── Employee/           # 员工管理
│   │   │   ├── Employee-list-view/
│   │   │   ├── Employee-info-view/
│   │   │   └── Employee-resetPwd-view/
│   │   ├── User/               # 用户管理
│   │   │   └── User-list-view/
│   │   ├── Passenger/          # 乘机人管理
│   │   │   └── Passenger-list-view/
│   │   ├── SeatReservation/    # 座位预订管理
│   │   │   ├── SeatReservation-list-view/
│   │   │   └── SeatReservation-dialog-view/
│   │   ├── Route/              # 航线管理
│   │   │   ├── Route-list-view/
│   │   │   └── Route-Station-Dialog/
│   │   ├── Station/            # 机场管理
│   │   │   └── Station-list-view/
│   │   └── Prediction/         # 客流预测
│   │       └── Prediction-view/
│   ├── App.vue                 # 根组件
│   └── main.ts                 # 应用入口
├── .env                        # 环境变量
├── .env.development            # 开发环境变量
├── .env.production             # 生产环境变量
├── index.html                  # HTML模板
├── package.json                # 项目配置
├── tsconfig.json               # TypeScript配置
├── vite.config.ts              # Vite配置
└── README.md                   # 项目说明
```

## 4. 路由结构

### 4.1 路由配置表

| 路径 | 组件 | 名称 | 权限 | 说明 |
|:-----|:-----|:-----|:----:|:-----|
| / | Layout | 布局 | 登录 | 系统主布局 |
| /home | Home | 首页 | 登录 | 管理后台首页 |
| /login | Login | 登录 | 公开 | 管理员登录 |
| /oauth2/callback | OAuth2Callback | 登录回调 | 公开 | OAuth2回调处理 |
| /airplane/list | AirplaneList | 飞机列表 | 登录 | 飞机管理 |
| /schedule/list | ScheduleList | 航班列表 | 登录 | 航班管理 |
| /order/list | OrderList | 订单列表 | 登录 | 订单管理 |
| /ticket/list | TicketList | 机票列表 | 登录 | 票务管理 |
| /employee/list | EmployeeList | 员工列表 | 登录 | 员工管理 |
| /employee/profile | Profile | 个人信息 | 登录 | 个人中心 |
| /employee/resetPassword | ResetPassword | 重置密码 | 登录 | 密码重置 |
| /user/list | UserList | 用户列表 | 登录 | 用户管理 |
| /passenger/list | PassengerList | 乘机人列表 | 登录 | 乘机人管理 |
| /seat-reservation/list | SeatReservationList | 座位预订列表 | 登录 | 座位预订管理 |
| /route/list | RouteList | 航线列表 | 登录 | 航线管理 |
| /station/list | StationList | 机场列表 | 登录 | 机场管理 |
| /prediction/analysis | PredictionAnalysis | 预测分析 | 登录 | 客流预测分析 |
| /:pathMatch(.*)* | NotFound | 404 | 公开 | 404页面 |

### 4.2 路由守卫

```typescript
// 路由前置守卫
router.beforeEach((to, from, next) => {
  // 设置页面标题
  document.title = `${to.meta.title || ''} - 飞机订票系统后台`

  // 支持 OAuth2 Token 和旧版 Token
  const token = localStorage.getItem('access_token') || localStorage.getItem('token')

  // 白名单路径（不需要登录）
  const whiteList = ['/login', '/oauth2/callback']

  if (whiteList.includes(to.path)) {
    next()
  } else if (!token) {
    next('/login')
  } else {
    next()
  }
})
```

## 5. 前后端交互流程

### 5.1 Axios封装

```typescript
// src/utils/request.ts - Axios请求封装

import axios, { type AxiosRequestConfig, type AxiosResponse } from 'axios'
import { ElMessage } from 'element-plus'
import type { ApiResponse, ClientType } from '@/types'
import { ClientType as ClientTypeEnum } from '@/types'
import config from '@/config'

// 请求拦截器
const requestInterceptor = (config: InternalAxiosRequestConfig) => {
  config.headers = config.headers || {}
  // 添加客户端类型请求头，标识当前为管理后台Web端
  config.headers['X-Client-Type'] = ClientTypeEnum.ADMIN_WEB
  // 添加Authorization头部
  const token = localStorage.getItem('access_token') || localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
}

// 响应拦截器
const responseInterceptor = (response: AxiosResponse<ApiResponse>) => {
  const { code, message, data } = response.data
  if (code === 200) {
    return data
  } else {
    const error = new Error(message || '请求失败')
    return Promise.reject(error)
  }
}

// 错误响应拦截器
const errorResponseInterceptor = (error: any) => {
  // 处理401未授权错误
  if (error.response?.status === 401) {
    localStorage.removeItem('access_token')
    localStorage.removeItem('refresh_token')
    localStorage.removeItem('token')
    window.location.href = '/login'
    ElMessage.error('登录已过期，请重新登录')
  } else {
    const errorMessage = error.response?.data?.message || error.message || '网络错误'
    ElMessage.error(errorMessage)
  }
  return Promise.reject(error)
}

// 创建请求实例
const createRequestInstance = (baseURL: string) => {
  const instance = axios.create({
    baseURL,
    timeout: config.timeout
  })
  
  instance.interceptors.request.use(requestInterceptor)
  instance.interceptors.response.use(responseInterceptor, errorResponseInterceptor)
  
  return instance
}

// 导出各个服务模块的请求实例
export const authRequest = createServiceRequest('auth')
export const userRequest = createServiceRequest('user')
export const airplaneRequest = createServiceRequest('airplane')
export const orderRequest = createServiceRequest('order')
export const predictionRequest = createServiceRequest('prediction')

export default request
```

### 5.2 API接口定义

```typescript
// src/api/prediction.ts - 预测服务API

import { predictionRequest } from '@/utils/request'
import type {
  PredictionRequest,
  PredictionResponse,
  ModelInfoResponse,
  QuickForecastParams,
  TrainingTaskResponse,
  TrainingTaskListParams,
  TrainingTaskListResponse,
  CancelTaskResponse,
  AutoRetrainStatusResponse
} from '@/types'

/**
 * 客流预测API
 */
export const forecast = (data: PredictionRequest): Promise<PredictionResponse> => {
  return predictionRequest.post('/forecast', data)
}

/**
 * 快速预测
 */
export const quickForecast = (params: QuickForecastParams): Promise<PredictionResponse> => {
  return predictionRequest.get('/forecast', { params })
}

/**
 * 获取模型信息
 */
export const getModelInfo = (): Promise<ModelInfoResponse> => {
  return predictionRequest.get('/model-info')
}

/**
 * 创建训练任务
 */
export const createTrainingTask = (): Promise<{ task_id: string; status: string; created_at: string }> => {
  return predictionRequest.post('/train/async')
}

/**
 * 获取训练任务状态
 */
export const getTrainingTaskStatus = (taskId: string): Promise<TrainingTaskResponse> => {
  return predictionRequest.get(`/train/task/${taskId}`)
}

/**
 * 获取训练任务列表
 */
export const getTrainingTaskList = (params: TrainingTaskListParams): Promise<TrainingTaskListResponse> => {
  return predictionRequest.get('/train/tasks', { params })
}

/**
 * 取消训练任务
 */
export const cancelTrainingTask = (taskId: string): Promise<CancelTaskResponse> => {
  return predictionRequest.post(`/train/task/${taskId}/cancel`)
}

/**
 * 获取自动重训状态
 */
export const getAutoRetrainStatus = (): Promise<AutoRetrainStatusResponse> => {
  return predictionRequest.get('/auto-retrain/status')
}
```

## 6. 组件结构

### 6.1 组件依赖关系

```
src/components/
│
├── DialogForm/                 # 对话框表单组件
│   ├── DialogForm.vue          # 依赖: Element Plus
│   └── DialogForm.scss         # 依赖: SCSS变量
│
├── Icon/                       # 自定义图标组件
│   ├── EmployeeIcon.vue        # 依赖: Vue
│   ├── OrderIcon.vue           # 依赖: Vue
│   ├── PassengerIcon.vue       # 依赖: Vue
│   ├── ProfileIcon.vue         # 依赖: Vue
│   ├── ResetPwdIcon.vue        # 依赖: Vue
│   ├── RouteIcon.vue           # 依赖: Vue
│   ├── ScheduleIcon.vue        # 依赖: Vue
│   ├── SeatReservationIcon.vue # 依赖: Vue
│   ├── TrainIcon.vue           # 依赖: Vue
│   └── TransportIcon.vue       # 依赖: Vue
│
└── ListPage/                   # 列表页面组件
    ├── ListPage.vue            # 依赖: Element Plus, Vue Router
    └── ListPage.scss           # 依赖: SCSS变量
```

### 6.2 布局组件

```
src/views/Layout.vue - 系统主布局

┌─────────────────────────────────────────────────────────────────┐
│  Header (顶部导航栏)                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  [折叠按钮]                                    [头像] [用户名] ▼ │   │
│  │                                                      个人中心 │   │
│  │                                                      重置密码 │   │
│  │                                                      退出登录 │   │
│  └─────────────────────────────────────────────────────────┘   │
├──────────────────┬──────────────────────────────────────────────┤
│                  │                                              │
│  Sidebar         │  Main Content                                │
│  (侧边菜单)       │  (主内容区)                                   │
│  ┌────────────┐  │  ┌────────────────────────────────────────┐  │
│  │ [Logo]     │  │  │                                        │  │
│  │ 飞机订票系统 │  │  │   页面内容                              │  │
│  ├────────────┤  │  │                                        │  │
│  │ 首页        │  │  │   - 数据表格                            │  │
│  │ 飞机管理    │  │  │   - 表单                                │  │
│  │ 航班管理    │  │  │   - 图表                                │  │
│  │ 订单管理    │  │  │   - 对话框                              │  │
│  │ 票务管理    │  │  │                                        │  │
│  │ 员工管理    │  │  │                                        │  │
│  │ 用户管理    │  │  └────────────────────────────────────────┘  │
│  │ 乘机人管理  │  │                                              │
│  │ 座位预订    │  │                                              │
│  │ 航线管理    │  │                                              │
│  │ 机场管理    │  │                                              │
│  │ 客流预测    │  │                                              │
│  └────────────┘  │                                              │
│                  │                                              │
└──────────────────┴──────────────────────────────────────────────┘
```

## 7. 状态管理

### 7.1 Pinia Store结构

```typescript
// src/stores/app.ts - 应用状态管理

import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useAppStore = defineStore('app', () => {
  // State
  const sidebarCollapsed = ref(false)
  
  // 菜单列表
  const menuList = ref([
    { path: '/home', title: '首页', icon: 'Home' },
    {
      path: '/airplane',
      title: '飞机管理',
      icon: 'Airplane',
      children: [
        { path: '/airplane/list', title: '飞机列表' }
      ]
    },
    {
      path: '/schedule',
      title: '航班管理',
      icon: 'Calendar',
      children: [
        { path: '/schedule/list', title: '航班列表' }
      ]
    },
    // ... 其他菜单
  ])
  
  // Actions
  const toggleSidebar = () => {
    sidebarCollapsed.value = !sidebarCollapsed.value
  }
  
  return {
    sidebarCollapsed,
    menuList,
    toggleSidebar
  }
})
```

```typescript
// src/stores/employee.ts - 员工状态管理

import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { EmployeeInfo } from '@/types'

export const useEmployeeStore = defineStore('employee', () => {
  // State
  const employeeInfo = ref<EmployeeInfo | null>(null)
  
  // Actions
  const setEmployeeInfo = (info: EmployeeInfo) => {
    employeeInfo.value = info
  }
  
  const logoutAction = () => {
    employeeInfo.value = null
    localStorage.removeItem('access_token')
    localStorage.removeItem('refresh_token')
    localStorage.removeItem('token')
  }
  
  return {
    employeeInfo,
    setEmployeeInfo,
    logoutAction
  }
}, {
  persist: true  // 持久化存储
})
```

## 8. 构建配置

### 8.1 Vite配置

```typescript
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    // 自动导入Vue API
    AutoImport({
      resolvers: [ElementPlusResolver()],
      imports: ['vue', 'vue-router', 'pinia']
    }),
    // 自动导入组件
    Components({
      resolvers: [ElementPlusResolver()]
    })
  ],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  },
  build: {
    target: 'esnext',
    outDir: 'dist'
  }
})
```

### 8.2 构建脚本

```json
{
  "scripts": {
    "dev": "vite",                                    // 开发环境启动
    "build:dev": "vite build --mode development",     // 开发环境构建
    "build:test": "vite build --mode staging",        // 测试环境构建
    "build:prod": "vite build --mode production",     // 生产环境构建
    "build": "vue-tsc --noEmit && vite build",        // 类型检查并构建
    "preview": "vite preview",                        // 预览构建结果
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix",
    "format": "prettier --write src/"
  }
}
```

## 9. 技术栈选型理由

### 9.1 为什么选择Vue 3 + Composition API？

1. **更好的TypeScript支持**：完整的类型推断和IDE支持
2. **Composition API**：更灵活的代码组织，更好的逻辑复用
3. **性能提升**：更小的包体积，更快的渲染性能
4. **生态成熟**：Element Plus、Vue Router、Pinia等生态完善

### 9.2 为什么选择Element Plus？

1. **企业级组件库**：提供完整的管理后台所需组件
2. **Vue 3原生支持**：完美适配Vue 3 Composition API
3. **主题定制**：支持自定义主题，符合企业品牌
4. **国际化**：内置中文支持

### 9.3 为什么选择Pinia？

1. **官方推荐**：Vue官方团队开发，未来趋势
2. **TypeScript友好**：完整的类型支持
3. **简洁API**：更简单的API设计，学习成本低
4. **持久化插件**：支持状态持久化

## 10. 总结

ATBS管理端前端技术栈采用Vue 3 + TypeScript + Element Plus的现代前端技术组合，具有以下特点：

1. **类型安全**：TypeScript全栈类型约束，减少运行时错误
2. **开发效率**：Vite极速构建 + 自动导入插件
3. **组件丰富**：Element Plus提供完整的企业级组件
4. **状态管理**：Pinia提供简洁高效的全局状态管理
5. **工程化**：ESLint + Prettier代码规范，Git版本控制

这种技术栈选择适合中大型管理后台项目的长期发展和维护。
